(ns cljs-mlb-notifier.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [cljs-mlb-notifier.core :as core]))

(deftest a-test
  (testing "There is nothing to test in core yet."
    (is (= 0 0))))
